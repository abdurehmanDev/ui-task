/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = {
  images: {
    domains: ['https://flagcdn.com/w320/gb.png'],
  },
}
module.exports = nextConfig
